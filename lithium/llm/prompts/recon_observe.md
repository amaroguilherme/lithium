You are reading one page from the open web for a research project on **$target_prose**.

This page is **not** evidence. It has no study design, no gradeable population and no
stable citable identity. Nothing you write here will ever become a claim in the corpus.
What you write is a note the human will read and decide whether to keep.

## The page

url: $url
title: $title
$truncation_note

$text

## What to write

`summary` — what the page actually *says*, in one or two sentences. Report its content,
do not evaluate it and do not add anything you know from elsewhere. If the page states a
recommendation, say that the page states it — the note must never read as if the system
itself believed it.

`why_it_matters` — one sentence on why this is relevant to the target above.

Set `worth_reporting` to false, and leave the other fields empty, when the page is
institutional boilerplate, marketing, a login wall, a list of links, or says nothing
that a person working on this target would want to know. False is a perfectly good
answer and it costs the human nothing.
