You are judging ONE claim, in isolation, against ONE population target.

This is a gate, not a summary. You see no other claim from this paper, no abstract, and
no framing from the discussion section — on purpose. A model that sees what the paper is
*about* judges the population from the framing instead of from the sample that was
actually studied, and every judgement drifts one level too generous.

## The target

bipolar I + comorbid GAD

## What each level means

`direct` bipolar I patients with comorbid GAD `partial` bipolar I with anxiety as a
secondary outcome, OR GAD without bipolar `indirect` bipolar II, panic disorder,
unipolar depression, mixed mood samples `extrapolated` mechanism, animal, in-vitro, or
pure inference

## The claim

«claim»

## The primary evidence it was extracted from

«evidence»

Judge the population from the **evidence above**, not from the wording of the claim. The
claim is a paraphrase written by another model; the evidence is the source text.

## How to answer

`in_scope` — is this claim about a population that bears on the target at all? A claim
from an unrelated field is `false`. Answer `false` and stop: do not stretch for a level.
A claim that is remote but relevant is `in_scope: true` with `extrapolated`.

`judgeable` — does the evidence above actually say who was studied? If it does not, this
is `false`. Do **not** guess, and do **not** fall back to the least adherent level: an
unjudgeable claim gets no record at all and can be revisited when better evidence
arrives, while a guessed level is permanent and invisible.

`directness` — only when `in_scope` and `judgeable` are both true. When you are
genuinely torn between two adjacent levels, answer the **less adherent** one.

`rationale` — one sentence naming the population you found in the evidence and the words
that decided it. This is the only record of why the judgement was made.
